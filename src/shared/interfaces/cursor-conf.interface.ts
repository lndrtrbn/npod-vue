import * as paper from "paper";

export interface CursorCircleConf {
    strokeColor: paper.Color;
    strokeColorHover: paper.Color;
    strokeWidth: number;
    segments: number;
    radius: number;
}